// Declare a `function` called `greet` that has 1 argument called `name` and returns a String `"Hi, <name>!"`
// This time use an arrow function

function greet(name) {
    return `Hi, ${name}!`
}

const greeting = greet('Elena');
console.log(greeting)